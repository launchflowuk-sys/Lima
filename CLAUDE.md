@AGENTS.md

# START HERE (new session)

**Read `HANDOFF.md` first.** It holds the state that isn't in the code — deployment (Hetzner + Coolify +
`agentlima.com`), infra access (SSH key), Docker build gotchas, env vars, and the pending work list.

Note: project auto-memory does **not** carry into this repo (it was saved under a different project scope),
so `HANDOFF.md` is the source of truth for cross-session context. Keep it updated as things change.
Your global `~/.claude` skills and instructions are available here as normal.

Feature status lives in `BUILD_PROGRESS.md`; how to run/test end-to-end is in `docs/RUNBOOK.md`.
